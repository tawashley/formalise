export default function validateMessageManager(inputElement, formConfig) {

    function getValidationMessage() {
        console.log('get validation message');
    }

    function init() {}

    init();

    return {
        getValidationMessage
    }
}
